name: Tests
on:
  push:
    paths:
      - 'packages/**'
      - 'tests/**'
      - '.github/workflows/tests.yml'
  pull_request:
    paths:
      - 'packages/**'
      - 'tests/**'
      - '.github/workflows/tests.yml'

defaults:
  run:
    shell: 'bash'

jobs:
  test:
    strategy:
      matrix:
        os: [ windows-latest ]

    runs-on: ${{ matrix.os }}

    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-node@v2
        with:
          node-version: 14

      - run: npm ci
      - run: npm test
